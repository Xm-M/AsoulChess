冰车僵尸：

1.只有移动状态，有一个CarArmor 会直接碾压植物（这些我自己能配置）

2.被动技能:移动时会在身后生成冰道

冰道：会覆盖原来的Tile 植物无法在上面种植，经过n秒后消失；设计方案：1.增加一种ice tiletype类型，冰车经过的时候直接改变地形；2.不改变tile 但是在原来地形上面铺上一层干扰鼠标Raycast检测的层，让鼠标点击的时候检测不到tile;3.直接将tile设置成UnSelectable;

冰道的实际形式：1.使用Prefab:好处是可以在这个Prefab上再挂载一个冰道的脚本 可以控制消失时间，如果被火爆辣椒什么的烧了也会直接消失。坏处就是不能像原版那样出现半格冰块的情况；2.使用Prefab但是是像烟雾.prefab一样，一行的冰道由一个冰道控制多个冰Tile组成；好处就是可以统一消失和出现，而且冰车也只用生成一个冰道 然后生成冰块就可以了；坏处也是不能生成半个冰道；3.就是用LineRender来实现冰道；好处就是可以随意控制长短，坏处就是如果有竖直的辣椒烧某块冰块的时候不好处理；

3.关于CarArmor:可能不能直接使用原来CarArmor的逻辑 首先冰车属于二类防御  会抵消子弹的buff(比如减速buff什么的) 就只受到伤害，其次就是造成伤害。这个可以先不做 