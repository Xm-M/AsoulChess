贪吃蛇（优先级 以Tn来表示）

1(T0).关卡控制：需要制作一个LevelController_Snake用于单独进行食物的生成，控制关卡的流程，判断关卡的获胜与否。

2(T1).蛇：可以做成chess 然后单独写一个FindTileMotethod_KeyBord 来控制蛇在Map上移动；吃到食物就在身后增加长度（生成SnakeUnit.Prefab）的逻辑可以写在Passive里面 碰撞逻辑可以额外写个SnakeArmor：BaseArmor来控制碰撞逻辑等 比如碰到食物层的单位就吃掉，碰到障碍物层的就失败；

3(T1).食物：制作成一个单独的类 因为后续可能有不同的食物类型 所以要可拓展  食物会做成预制体 在levelController里生成；食物需要稀有度 根据关卡进行来增加难度

4(T1).蛇身：有个跟随蛇移动的特点。不知道是单独写一个移动脚本跟随蛇移动 还是在蛇的FindTileMotethod_KeyBord用一个List<GameObject> SnakeBody来控制整个身体移动；蛇身的layer也是 deathpool 因为撞自己的身体也算失败

5(T2).障碍物：不需要单独的逻辑 只需要挂上deathpool的layer就行了

6.(T3)敌人：enemy层 碰撞也会失败 可能会主动追蛇 

暂时就想到这些 有需要补充的可以放在这里