僵王

1.僵王有四个阶段
高于80%血量时 全身完整，站立状态只有放置僵尸一种方式

50%~80%的时候 头部，下巴，脚部出现破损（贴图变化），站立状态增加放蹦极僵尸技能 （一次站立会放置七个僵尸+一次放蹦极）

10%~50%的时候 头部，下巴，脚步出现更多破损（贴图变化），出现冒烟（挂载的冒烟特效SetActive(true)） 站立状态增加丢车和脚踩两个技能；召唤僵尸的速度变快 （一次砸车 和 14只僵尸）

低于10%的时候 没有贴图变化 但是僵王的全身会一闪一闪的；

2.僵王有五个技能

站立阶段：

一、召唤僵尸：具体召唤位置是

```c#
int x = user.moveController.standTile.mapPos.x + (int)user.transform.right.x * 2;
int y = random.range(0,Mathf.Max(5,Mapmanage.instance.size.y-1));//这个其实应该是随机的
Tile tile = MapManage.instance.tiles[x, y];
```

根据召唤行的不同 播放不同的召唤动画。

召唤的种类与俯身次数有关 有List<Property> zombieTypes和List<Property>ZombieCanSummons;当俯身次数*2>zombieTypes[n].baseproperty.waveLimit 则将其加入ZombieCanSummons
生成的僵尸在ZombieCanSummons随机选择

二、召唤蹦极：在三个随机植物的位置召唤蹦极僵尸
三、脚踩：会踩中的tile是

```
int x = user.moveController.standTile.mapPos.x + (int)user.transform.right.x * 1;
int y = random.range(0,Mathf.Max(4,Mapmanage.instance.size.y-1));
//踩到的Tile
Tile tile = MapManage.instance.tiles[x, y];
Tile tile = MapManage.instance.tiles[x+1, y];
Tile tile = MapManage.instance.tiles[x+2, y];
Tile tile = MapManage.instance.tiles[x, y+1];
Tile tile = MapManage.instance.tiles[x+1, y+1];
Tile tile = MapManage.instance.tiles[x+2, y+1];
```

根据召唤行的不同 播放不同的脚踩动画。

四、砸车：按理来说应该也有四个动画对应位置的现在没有了 就只需要消灭int x = user.moveController.standTile.mapPos.x + (int)user.transform.right.x * 6; int y=1;然后对应的六个格子

俯身状态：只有一个技能：



```
int x = user.moveController.standTile.mapPos.x + (int)user.transform.right.x * 3;
int y = random.range(0,Mathf.Max(4,Mapmanage.instance.size.y-1));
Tile tile = MapManage.instance.tiles[x, y];
```

在tile位置生成一个火球/冰球（随机二选一）；

根据生成位置（y）播放不同的动画

根据生成的是冰球还是火球 更换眼睛、嘴部的贴图

3.关于状态切换

站立状态根据行为生命值不同有不同的行为；进入站立阶段时无法选中，清除所有buff。

生命值高于50%的时候：

召唤僵尸：每4.5s使用一次 一次站立召唤7只

会使用一次召唤蹦极技能；顺序随机

共使用7次召唤僵尸和一次召唤蹦极 所有技能使用完毕后2s切换为俯身状态

低于50%的时候：

召唤僵尸：每3s使用一次召唤 一次站立召唤14只

会使用一次召唤蹦极或者砸车；

如果脚踩技能范围（IFindTarget）内有目标，则使用一次脚踩技能；否则不使用。

 所有技能使用完毕后2s切换为俯身。

俯身状态：进入俯身状态时，可以选中；10s后使用吐火球；经过20s切换到站立状态。实际的计算时间为t+=Time.detalTime*AcceleRate；也就是减速和冰冻会延长时间



