# Entity Demo

1. Package Manager → **AsoulChess Game Entity** → Samples → **Entity Demo** → Import  
2. Empty scene → empty GameObject → add `EntityDemoRunner`  
3. Play → press **Space** several times until Dummy dies  

Expect ATK buff + damage logs, then death.

AVZ `Chess` is **not** migrated to this package.
