雪橇僵尸

1.只出现在冰道上 雪橇损坏前，以一个整体在冰道上快速移动

2.雪橇损坏或离开冰道后 四个雪橇僵尸独立行动

3.雪橇移动时 四只僵尸无法被控制 受到的伤害由雪橇车承担

4.防具300 本体270 临界值90的召唤系僵尸；价值为75（相当于3只普僵）

5.实际设计：

雪橇僵尸由雪橇防具与本体组合而成；一开始只有一只僵尸 后面坐的三只是图片；在防具被破坏后在对应位置生成另外三只雪橇僵尸；

雪橇在离开冰道后会消失（就是被破坏）

可以制作一个被动技能：

(1)给予一个加速buff

(2)注册雪橇Armor的Broken事件，召唤3只普通的雪橇僵尸,加速buff结束；


---

## 工程实现说明（脚本）

| 脚本 | 作用 |
|------|------|
| `Assets/Script/Weapon/Armor/Armor_Sled.cs` | 雪橇防具 300；`BrokenArmor` 触发 `OnArmorBroken`（离开冰道时被动调用 `BrokenArmor`，与被打爆同一条链） |
| `Assets/Script/Skill/ISkillPassive/Zombie/PassiveSkillEffect_SledZombie.cs` | 被动内完成：加速、不可选中；`OnReachTile` 无冰则 `BrokenArmor`；`OnArmorBroken` 结束加速并刷步行尸（默认 3 只 + 本体 = 4） |

**预制体配置要点**

1. 棋子挂 `PassiveSkill`，`effect` 指向 `PassiveSkillEffect_SledZombie`（配置 `sledSpeedBuff`、`footZombieCreator`、数量默认 3）。
2. `MoveController.tileMethod` 使用 **`HorMove`**（或其它常规寻路）；冰道由被动 `OnReachTile` 判断。
3. 子物体挂 **`Armor_Sled`**，与本体 `Chess` 的 `ArmorBase.user` 一致（同 HeadArmor 用法）。
4. 关卡需有 **`Effect_Snow`** 铺冰；离开冰格会触发 `BrokenArmor` 与刷怪。
5. 另建 **`PropertyCreator`（步行雪橇尸）**：无雪橇、本体数值按策划（270/90 等），供解散时生成。

